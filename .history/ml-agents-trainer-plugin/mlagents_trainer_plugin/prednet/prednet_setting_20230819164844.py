# from mlagents.trainers.settings import TrainerSettings, HyperparamSettings

# class PrednetSettings(HyperparamSettings):
#     # Define any settings required for your trainer here.
#     # It seems you don't need any hyperparameters, so this can be empty for now.
#     pass

# class PrednetTrainerSettings(TrainerSettings):
#     hyperparameters = PrednetSettings()
